'use Strict';

module.exports = () => {
  const checkPasswordRestrict = (password) => new Promise((resolve, reject) => {
    //Make a regex to password restrict 
    resolve(password)
  })

  const cryptPassword = (password) => new Promise((resolve, reject) => {

  })

  const checkPassword = (password) => new Promise((resolve, reject) => {

  })

  return {
    checkPasswordRestrict, 
    cryptPassword, 
    checkPassword
  }
}
'use strict'

const userNameAvailabilitySchema = require('./userNameAvailabilitySchema')
const emailAvailabilitySchema = require('./emailAvailabilitySchema')

module.exports = {
  userNameAvailabilitySchema,
  emailAvailabilitySchema
}